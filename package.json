{
  "name": "real-client-ip",
  "version": "1.0.2",
  "description": "Read real client IP address from proxy headers",
  "license": "MIT",
  "author": {
    "name": "Fabeuluci"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fabeuluci/real-client-ip.git"
  },
  "main": "out/ClientIP.js",
  "types": "out/ClientIP.d.ts",
  "scripts": {
    "compile": "rm -rf out && tsc",
    "watch": "rm -rf out && tsc -w",
    "test": "q2-test"
  },
  "devDependencies": {
    "@types/node": "^17.0.45",
    "@types/proxy-addr": "^2.0.0",
    "q2-test": "^1.0.1"
  }
}
